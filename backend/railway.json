{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "backend/Dockerfile"
  },
  "deploy": {
    "startCommand": "npx prisma db push --skip-generate --accept-data-loss ; echo DIAG_PRISMA_EXIT=$? ; node dist/src/main ; echo DIAG_NODE_EXIT=$?",
    "restartPolicyType": "ON_FAILURE"
  }
}
