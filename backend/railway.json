{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "backend/Dockerfile"
  },
  "deploy": {
    "startCommand": "npx prisma db push --skip-generate && (node prisma/seed.cjs || true) && node dist/src/main",
    "restartPolicyType": "ON_FAILURE"
  }
}
