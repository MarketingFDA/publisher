{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "backend/Dockerfile"
  },
  "deploy": {
    "startCommand": "PRISMA_HIDE_UPDATE_MESSAGE=1 npx prisma db push --skip-generate --accept-data-loss && exec node dist/src/main",
    "restartPolicyType": "ON_FAILURE"
  }
}
