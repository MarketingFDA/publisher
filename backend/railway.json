{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm install && npm run build && npx prisma generate && cp -r /app/backend/dist /app/dist && cp -r /app/backend/node_modules /app/node_modules"
  },
  "deploy": {
    "startCommand": "cd backend && npx prisma db push --skip-generate && node /app/dist/main",
    "restartPolicyType": "ON_FAILURE"
  }
}
