{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "npx prisma db push --skip-generate && (node prisma/seed.cjs || true) && node dist/main",
    "restartPolicyType": "ON_FAILURE"
  }
}
