{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "backend/Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c \"npx prisma db push --skip-generate --accept-data-loss && node prisma/seed.cjs && node dist/src/main\"",
    "restartPolicyType": "ON_FAILURE"
  }
}
